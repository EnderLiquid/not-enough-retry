{
  "name": "not-enough-retry",
  "version": "0.1.0",
  "description": "Make Pi's built-in retry catch every non-permanent provider error.",
  "type": "module",
  "license": "MIT",
  "author": "EnderLiquid",
  "homepage": "https://github.com/EnderLiquid/not-enough-retry#readme",
  "bugs": {
    "url": "https://github.com/EnderLiquid/not-enough-retry/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/EnderLiquid/not-enough-retry.git"
  },
  "keywords": [
    "pi-package",
    "pi-extension",
    "pi-coding-agent",
    "retry",
    "provider-error",
    "backoff"
  ],
  "files": [
    "extensions",
    "README.md",
    "LICENSE"
  ],
  "exports": {
    ".": "./extensions/not-enough-retry.ts"
  },
  "pi": {
    "extensions": [
      "./extensions/not-enough-retry.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": ">=0.84.0"
  },
  "scripts": {
    "test": "tsx --test tests/*.test.ts",
    "test:types": "tsc --noEmit"
  },
  "devDependencies": {
    "@earendil-works/pi-agent-core": "^0.84.1",
    "@earendil-works/pi-ai": "^0.84.1",
    "@earendil-works/pi-coding-agent": "0.84.1",
    "@types/node": "^24.10.0",
    "tsx": "^4.23.10",
    "typescript": "^5.9.3"
  }
}
